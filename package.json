{
  "name": "loft",
  "version": "0.1.0",
  "description": "The Loft: A digital sanctuary for Gen Z tech workers",
  "private": true,
  "packageManager": "npm@11.11.0",
  "workspaces": ["frontend", "services/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "migrate": "ts-node packages/db-schema/migrate.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "ts-node": "^10.9.2",
    "@types/pg": "^8.11.6",
    "pg": "^8.13.1",
    "typescript": "^5.0.0"
  }
}
